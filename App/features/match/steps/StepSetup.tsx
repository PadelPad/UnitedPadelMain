import { View, Text, Pressable } from 'react-native'; import { useMatchDraft } from '@/lib/state/useMatchDraft'; import { Button } from '@/components/ui/Button'; export function StepSetup({onNext}:{onNext:()=>void}){const {draft,setField}=useMatchDraft();return(<View className='flex-1 p-4 gap-3'><Text className='text-white text-lg font-semibold'>1. Match setup</Text><View className='flex-row gap-2'>{(['singles','doubles'] as const).map(v=>(<Pressable key={v} onPress={()=>setField('matchType',v)} className={`px-4 py-3 rounded-xl ${draft.matchType===v?'bg-orange-500':'bg-white/10'}`}><Text className='text-white'>{v}</Text></Pressable>))}</View><View className='flex-row gap-2'>{(['friendly','league','tournament','nationals'] as const).map(v=>(<Pressable key={v} onPress={()=>setField('matchLevel',v)} className={`px-3 py-2 rounded-xl ${draft.matchLevel===v?'bg-orange-500':'bg-white/10'}`}><Text className='text-white'>{v}</Text></Pressable>))}</View><Button title='Next' onPress={onNext}/></View>);}