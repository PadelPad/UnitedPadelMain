import { useState } from 'react'; import { View } from 'react-native'; import { StepSetup } from './steps/StepSetup'; import { StepPlayers } from './steps/StepPlayers'; import { StepScores } from './steps/StepScores'; import { StepSummary } from './steps/StepSummary'; export function Stepper(){const [step,setStep]=useState(0);return(<View className='flex-1'>{step===0&&<StepSetup onNext={()=>setStep(1)}/>} {step===1&&<StepPlayers onPrev={()=>setStep(0)} onNext={()=>setStep(2)}/>} {step===2&&<StepScores onPrev={()=>setStep(1)} onNext={()=>setStep(3)}/>} {step===3&&<StepSummary onPrev={()=>setStep(2)}/>}</View>);}